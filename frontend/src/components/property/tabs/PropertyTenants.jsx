/ components/property/tabs/PropertyTenants.jsx
export const PropertyTenants = ({ property, onViewTenant, onEditTenant, onDeleteTenant }) => {
  return (
    <div className="space-y-6">
      {/* Tenants content */}
    </div>
  );
};
